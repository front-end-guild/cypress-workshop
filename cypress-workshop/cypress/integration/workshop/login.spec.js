/// <reference types="Cypress" />

context('Login', () => {
  beforeEach(() => {
    cy.visit('http://localhost:4200');
  });

  it('login and route to the users page succussfully', () => {
  });
});
